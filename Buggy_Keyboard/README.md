Generally, pressing the backspace key should erase a character to the left of the cursor. But on this
computer, pressing that key produces a character <.
Input
A string s containing text written on Gabrielle’s computer. The length of s is less than 10ˆ6 The string will contain
only lower case letters, spaces, and the character <.
Output
A string containing text that was actually intended. Note that there should be no spaces or new line after the last
character in the output
