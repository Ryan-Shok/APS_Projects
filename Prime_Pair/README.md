A prime pair is a pair of the form (p, p + 2) where p and (p + 2) are both prime.

Input:
The input will contain a single integer N (1 <= N <= 100,000).

Output:
The output should be a single line, the N’th prime pair formatted as (p1, p2).