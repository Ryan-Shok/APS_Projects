Input:
The input consists of a single integer n between 1 and 2ˆ31- 1 written in standard decimal (base 10) format on a
single line.

Output:
The output consists of a single line, containing the integers a(n) and b(n) separated by a single space. Both a(n)
and b(n) should be written in decimal format